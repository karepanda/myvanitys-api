# Configuración específica para entorno de producción
spring:
  datasource:
    hikari:
      connectionTimeout: 30000
      maximumPoolSize: 10
      minimumIdle: 5
      idleTimeout: 600000
      maxLifetime: 1800000
    # PostgreSQL configuración para Railway
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    password: ${PGPASSWORD}

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: false

# Configuración para OAuth en producción
google:
  oauth:
    redirect-uri: ${OAUTH_REDIRECT_URI:https://myvanitys.com/callback}

# Configuración de seguridad para producción
management:
  security:
    enabled: true  # Habilitar seguridad en actuator para producción

# Configuración de logging para producción
logging:
  level:
    org:
      springframework: WARN
      flywaydb: INFO
      hibernate: WARN