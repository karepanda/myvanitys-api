server:
  servlet:
    context-path: /myvanitys
spring:
  profiles:
    active: dev

management:
  server:
    base-path: /actuator
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: "*"
info:
  app:
    name: MyVanitys API
    version: ${project.version}
